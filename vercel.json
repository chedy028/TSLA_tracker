{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npm run verify:release; else npm run verify:i18n; fi && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}




